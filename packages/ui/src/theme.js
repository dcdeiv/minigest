import { createMuiTheme } from "@material-ui/core/styles";

export const defaultTheme = createMuiTheme({
  common: {
    white: "#F8F8F8",
    black: "#2B2B2B",
  },
  palette: {
    type: "dark",
  },
});
