import { loginSuccess, loginFail, login } from "./login";
import { getSuccess, getFail, get } from "./get";

export const utente = {
  loginSuccess,
  loginFail,
  login,
  getSuccess,
  getFail,
  get,
};
