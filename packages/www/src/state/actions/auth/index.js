import { loginSuccess, loginFail, login } from "./login";
import { getStart, getSuccess, getFail, get } from "./get";
import { putStart, putSuccess, putFail, put } from "./put";
import { pwChangeSuccess, pwChangeFail, pwChange } from "./pwChange";

export const auth = {
  loginSuccess,
  loginFail,
  login,
  getStart,
  getFail,
  getSuccess,
  get,
  putStart,
  putSuccess,
  putFail,
  put,
  pwChangeSuccess,
  pwChangeFail,
  pwChange,
};
