import { getStart, getFail, getSuccess, get } from "./get";
import { postStart, postFail, postSuccess, post } from "./post";
import { putStart, putFail, putSuccess, put } from "./put";
import { removeStart, removeFail, removeSuccess, remove } from "./remove";

export const regimeFiscale = {
  getStart,
  getFail,
  getSuccess,
  get,
  postStart,
  postFail,
  postSuccess,
  post,
  putStart,
  putFail,
  putSuccess,
  put,
  removeStart,
  removeFail,
  removeSuccess,
  remove,
};
