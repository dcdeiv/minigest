import { getStart, getFail, getSuccess, get } from "./get";
import { getQstart, getQfail, getQsuccess, getQ } from "./getq";

export const ivaAliquote = {
  getStart,
  getFail,
  getSuccess,
  get,
  getQstart,
  getQfail,
  getQsuccess,
  getQ,
};
